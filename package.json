{
  "name": "cline-fleet-control-plane",
  "version": "1.0.0",
  "description": "Config-driven local multi-agent control plane with pluggable worker runtimes.",
  "private": true,
  "type": "module",
  "bin": {
    "cline-fleet": "./src/cli.js"
  },
  "scripts": {
    "check": "for f in src/*.js; do node --check \"$f\" || exit 1; done",
    "start": "node src/cli.js studio",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
